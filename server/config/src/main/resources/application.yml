server:
  port: 8888
spring:
  cloud:
   config:
    server:
     git:
      uri: https://github.com/VerRan/cloud-config
eureka:
  client:
    serviceUrl:
      defaultZone: http://114.115.172.242:8761//eureka/
  instance:
    preferIpAddress: true
#####设置日志######
logging:
   file: ..//logs//config.log
   level:
     root: info